# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

<aside>

Чтобы составить документ с описанием текущей архитектуры приложения, можно часть информации взять из описания компании и условия задания. Это нормально.

</aside

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Пользователи могут управлять отоплением в доме.

**Мониторинг температуры:**

- Пользователи могут проверять температуру
  Каждая установка сопровождается выездом специалиста по подключению системы отопления в доме к текущей версии системы.
  Самостоятельно подключить свой датчик к системе пользователь не может.


### 2. Анализ архитектуры монолитного приложения

Архитектура приложения представляет из себя монолит на Go с СУБД Postgres. Всё синхронно. Никаких асинхронных вызовов, микросервисов и реактивного взаимодействия в системе нет. Всё управление идёт от сервера к датчику. Данные о температуре также получаются через запрос от сервера к датчику.

### 3. Определение доменов и границы контекстов целевого решения

Домен управления отоплением

Домен мониторинга
	Поддомен мониторинга температуры

Домен управление пользователями

Домен обработки сценариев автоматизации 

Домен сбора данных и аналитики

Доме интеграции с внешними устройствами

### **4. Проблемы монолитного решения**

Производительность
    Горизонтальное масштабирование невозможно. При росте количества подключённых домов в рамках экосистемы умных посёлков вся нагрузка будет ложиться на один сервер. 	
    Синхронные запросы к датчикам создают блокировки. При опросе большого количества датчиков на сервере выстроится очередь, что замедлит работу всей системы.
    Отсутствие оптимизации под разные нагрузки. Компоненты чтения температуры и управления отоплением имеют разные паттерны использования, но вынуждены работать в рамках одной системы.
Надёжность и отказоустойчивость
    Единая точка отказа. Если монолитное приложение выходит из строя, все пользователи теряют возможность управлять отоплением одновременно.
    Проблема в любом компоненте может привести к падению всей системы. Например, зависший запрос к одному датчику может заблокировать обработку запросов от всех остальных пользователей.
    Невозможность изолированного восстановления. При сбое нужно перезапускать всё приложение целиком, что увеличивает время простоя.
    Отсутствие резервирования на уровне компонентов. Нельзя создать резервные инстансы только для критически важных функций.
Разработка и развитие
    Монолитная кодовая база усложняет разработку. Разные команды не могут работать независимо над разными компонентами системы.
    Медленные релизы. Любое изменение требует пересборки, тестирования и развёртывания всего приложения, что замедляет внедрение новых функций.
    Сложность мониторинга. Трудно выделить проблемы в конкретных компонентах системы, так как всё работает в рамках одного процесса.
    Проблемы с развёртыванием. Обновление системы требует полной остановки сервиса для всех пользователей.
Бизнес процессы
    Каждая установка сопровождается выездом специалиста по подключению системы отопления в доме к текущей версии системы.
    Самостоятельно подключить свой датчик к системе пользователь не может.


### 5. Визуализация контекста системы — диаграмма С4

Добавьте сюда диаграмму контекста в модели C4.

Чтобы добавить ссылку в файл Readme.md, нужно использовать синтаксис Markdown. Это делают так:

```markdown
[Context](diagrams\Context.puml)
```

# Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

```markdown
[Container](diagrams\Container.puml)
```

**Диаграмма компонентов (Components)**

```markdown
[Component](diagrams\Component.puml)
```

**Диаграмма кода (Code)**

```markdown
[Code](diagrams\Code.puml)
```

# Задание 3. Разработка ER-диаграммы

```markdown
[ER](diagrams\ER.puml)
```

# Задание 4. Создание и документирование API

### 1. Тип API

Приложение использует как синхронное API (например, запрос значения температуры), так и асинхронное (например, обработка сценариев через Message Broker)

### 2. Документация API

http://localhost:8081/api-docs
http://localhost:8081/swagger-ui/index.html

# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.


